Analyze the supplied pipeline metadata and bounded logs. Classify the failure and propose a code change only when evidence supports one. Never treat infrastructure or credential errors as code-fix proposals without evidence. Return JSON.
{{context}}

