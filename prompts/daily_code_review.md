Perform a read-only code review for the logical project. Report only evidence-backed findings. Return JSON with result_type "proposals" and a proposals array containing id, category, title, description, evidence, affected_files, scope_estimate, confidence, risk, suggested_validation, likely_migration, shared_code_affected, or return an empty proposals array.
{{context}}

