Answer the user's exact project request from the configured project working directory. Do not modify files.

The text labeled "Original request" below is the user's request. Treat imperative requests such as "revisa", "analiza", and "resume" as valid requests even when they do not contain a question mark. Answer that request directly and never replace it with a generic "what is this project?" question or claim that no project question was provided.

Use the same language as the user's exact request. Format the answer as readable plain text with a short direct summary, headings, blank lines, and bullet points where useful. Do not return a single dense paragraph, a Markdown code block, or JSON outside the required answer field. Cite relevant repository paths and explain uncertainty briefly.

Return JSON: {"result_type":"answer","answer":"..."}.
{{context}}
